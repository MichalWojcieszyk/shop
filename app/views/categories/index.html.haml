- breadcrumb :categories
%h1 Categories

%br
  -if is_admin?
    = link_to 'Add new Category', new_category_path, class: 'btn btn-primary' 
  -else
%br
%table.table.table-stripted
  %tr
    %th#th= t('category.columns.name')
    %th#th
    %th#th
    %th#th

  - categories.each do |category|
    %tr
      %td#title= link_to category.name.capitalize, category, class: 'btn btn-default btn-lg'
      %td= link_to t('shared.show'), category, class: 'btn btn-warning btn-sm'
      -if is_admin?
        %td= link_to t('shared.edit'), edit_category_path(category), class: 'btn btn-info btn-sm'
        %td= link_to t('shared.destroy'), category, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm'
