%h1 Products

%table.table
  %tr
    %th#th Title
    %th#th Description
    %th#th Price
    %th#th
    %th#th
    %th#th

  - products.each do |product|
    %tr
      %td= link_to product.title.capitalize, category_product_url(category, product)
      %td=raw product.description.capitalize
      %td= product.price
      -if user_signed_in?
        %td= link_to 'Show', category_product_path(category.id, product.id), class: 'btn btn-warning btn-sm'
        -if current_user.id == product.user_id
          %td= link_to 'Edit', edit_category_product_path(category.id, product.id), class: 'btn btn-info btn-sm'
          %td= link_to 'Destroy', category_product_path(category.id, product.id), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm'