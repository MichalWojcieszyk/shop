%h1 Edit profile

= simple_form_for(user, html: {multipart: true}) do |f|
  = f.input :firstname
  = f.input :lastname
  = f.input :avatar
  %br
  = f.submit t('shared.save'), class: 'btn btn-success'
  