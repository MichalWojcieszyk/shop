-#= binding.pry
%h1 Your profile

.panel.panel-default
  .panel.panel-primary#threviews
    First name:
  .panel-body#userpadding#usercontent
    = user.firstname
    
.panel.panel-default
  .panel.panel-primary#threviews
    Last name:
  .panel-body#usercontent
    = user.lastname

.panel.panel-default
  .panel.panel-primary#threviews
    Nickname:
  .panel-body#usercontent
    = user.nickname
    
.panel.panel-default
  .panel.panel-primary#threviews
    Email:
  .panel-body#usercontent
    = user.email
  
.panel.panel-default
  .panel.panel-primary#threviews
    Account created at:
  .panel-body#usercontent
    = user.created_at.strftime('%d-%m-%Y')

.panel.panel-default
  .panel.panel-primary#threviews
    Your account balance:
  .panel-body#usercontent
    = user.cash_amount
    %br 
    %br
    = link_to 'Make a deposit', new_deposit_path, class: 'btn btn-primary'

.panel.panel-default
  .panel.panel-primary#threviews
    Avatar:
  .panel-body#usercontent
    = avatar_for(user)

%br
-if current_user.products.empty?
  #empty You haven't got any products.
-else
  %h2#center Your products
  %br
  %table.table.table-stripted
    %tr
      %th#threviews Product:
      %th#threviews Price:
      %th#threviews Quantity:
      %th#threviews Time:
    - current_user.products.reverse_order.last(15).each do |product|
      %tr
        %td= product.title#, category_product_path(product.category, product)
        %td= product.price
        %td= product.on_stock
        %td= product.created_at.strftime('%d-%m-%Y %H:%M:%S')

%br
-if current_user.payments.empty?
  #empty You haven't bought any products.
-else
  %h2#center Your last purchases
  %br
  %table.table.table-stripted
    %tr
      %th#threviews Product:
      %th#threviews Price:
      %th#threviews Quantity:
      %th#threviews Amount:
      %th#threviews Time:
    - current_user.payments.reverse_order.last(5).each do |payment|
      %tr
        %td= link_to payment.product.title, category_product_path(product.category, payment.product)
        %td= payment.product.price
        %td= payment.quantity
        %td= payment.amount
        %td= payment.created_at.strftime('%d-%m-%Y %H:%M:%S')

%br
-if current_user.reviews.empty?
  #empty You haven't got any reviews
-else
  %h2#center Your last reviews
  %br
  %table.table.table-stripted
    %tr
      %th#threviews Product:
      %th#threviews Review content:
      %th#threviews Review rating:
      %th#threviews Created at:
    - current_user.reviews.reverse_order.last(5).each do |review|
      %tr
        %td= review.product.title#link_to review.product.title, category_product_path(product.category, review.product)
        %td= review.content
        %td= review.rating
        %td= review.created_at.strftime('%d-%m-%Y %H:%M:%S')

%br
-if current_user.deposits.empty?
  #empty You haven't made any deposits.
-else
  %h2#center Your last deposits
  %br
  %table.table.table-stripted
    %tr
      %th#threviews Deposit:
      %th#threviews Time:
    - current_user.deposits.last(5).each do |deposit|
      %tr
        %td= deposit.amount
        %td= deposit.created_at.strftime('%d-%m-%Y %H:%M:%S')
=link_to "Edit", edit_user_path(current_user), class: 'btn btn-primary'
  